# Setting Up a Python Virtual Environment

To create a Python virtual environment and install the required packages, follow these steps:

```bash
python3 -m venv venv

source venv/bin/activate

pip install -r requirements.txt
```
