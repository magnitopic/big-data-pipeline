version: "3.9"

services:

  #####################################################################
  # ✅ NIFI — INGESTA / ORQUESTACIÓN
  #####################################################################
  nifi:
    container_name: nifi
    image:  apache/nifi:1.21.0
    restart: always
    ports:
        - "8080:8080"
    environment:
        NIFI_WEB_HTTP_PORT: 8080
        NIFI_WEB_HTTP_HOST: 0.0.0.0
    volumes:
    - ./data:/opt/nifi/data
  
  #####################################################################
  # ✅ ZOOKEEPER — REQUERIDO POR KAFKA
  #####################################################################
  zookeeper:
    container_name: zookeeper
    image: confluentinc/cp-zookeeper:7.5.0
    hostname: zookeeper
    networks:
      - network
    restart: always
    ports:
      - "2181:2181"
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    volumes:
      - zookeeper-data:/var/lib/zookeeper/data
      - zookeeper-logs:/var/lib/zookeeper/log


  #####################################################################
  # ✅ KAFKA — STREAMING
  #####################################################################
  kafka:
    container_name: kafka
    build:
      context: ./Docker/kafka/.
    image: kafka
    hostname: kafka
    networks:
      - network
    restart: always
    env_file: .env
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "false"
    volumes:
      - kafka-data:/var/lib/kafka/data
    depends_on:
      - zookeeper


  #####################################################################
  # 💤 BASES DE DATOS — DESACTIVADAS (ACTIVAR SOLO SI SE USAN)
  #####################################################################

  # mariadb:
  #   container_name: mariadb
  #   build:
  #     context: ./Docker/mariadb/.
  #   image: mariadb
  #   networks:
  #     - network
  #   restart: always
  #   env_file: .env
  #   ports:
  #     - "3306:3306"

  # mongodb:
  #   container_name: mongodb
  #   build:
  #     context: ./Docker/mongodb/.
  #   image: mongodb
  #   hostname: mongodb
  #   networks:
  #     - network
  #   restart: always
  #   env_file: .env
  #   ports:
  #     - "27017:27017"

  # cassandra:
  #   container_name: cassandra
  #   build:
  #     context: ./Docker/cassandra/.
  #   image: cassandra
  #   hostname: cassandra
  #   networks:
  #     - network
  #   restart: always
  #   ports:
  #     - "9042:9042"


  #####################################################################
  # 💤 SPARK — DESACTIVADO (ACTIVAR PARA BATCH PROCESSING)
  #####################################################################

  # spark-master:
  #   container_name: spark-master
  #   build:
  #     context: ./Docker/spark/.
  #   image: spark
  #   hostname: spark-master
  #   networks:
  #     - network
  #   restart: always
  #   ports:
  #     - "7077:7077"
  #     - "8081:8080"
  #   environment:
  #     SPARK_MODE: master

  # spark-worker:
  #   container_name: spark-worker
  #   build:
  #     context: ./Docker/spark/.
  #   image: spark
  #   hostname: spark-worker
  #   networks:
  #     - network
  #   restart: always
  #   ports:
  #     - "8082:8081"
  #   environment:
  #     SPARK_MODE: worker
  #     SPARK_MASTER_URL: spark://spark-master:7077
  #   depends_on:
  #     - spark-master


  #####################################################################
  # 💤 HDFS — DESACTIVADO (ACTIVAR SOLO SI REALMENTE SE USA)
  #####################################################################

  # hdfs-namenode:
  #   container_name: hdfs-namenode
  #   build:
  #     context: ./Docker/hdfs/.
  #   image: hdfs
  #   hostname: hdfs-namenode
  #   networks:
  #     - network
  #   restart: always
  #   ports:
  #     - "9870:9870"
  #     - "8020:8020"

  # hdfs-datanode:
  #   container_name: hdfs-datanode
  #   build:
  #     context: ./Docker/hdfs/.
  #   image: hdfs
  #   hostname: hdfs-datanode
  #   networks:
  #     - network
  #   restart: always
  #   ports:
  #     - "9864:9864"
  #   depends_on:
  #     - hdfs-namenode


  #####################################################################
  # 💤 FLINK — DESACTIVADO (ACTIVAR PARA STREAM PROCESSING)
  #####################################################################

  # flink-jobmanager:
  #   container_name: flink-jobmanager
  #   build:
  #     context: ./Docker/flink/.
  #   image: flink
  #   hostname: flink-jobmanager
  #   networks:
  #     - network
  #   restart: always
  #   ports:
  #     - "8083:8081"
  #     - "6123:6123"

  # flink-taskmanager:
  #   container_name: flink-taskmanager
  #   build:
  #     context: ./Docker/flink/.
  #   image: flink
  #   hostname: flink-taskmanager
  #   networks:
  #     - network
  #   restart: always
  #   depends_on:
  #     - flink-jobmanager

volumes:
  nifi-conf:
  nifi-database:
  nifi-flowfile:
  nifi-content:
  nifi-provenance:
  nifi-state:
  nifi-logs:
  mongodb-data:
  mongodb-config:
  kafka-data:
  zookeeper-data:
  zookeeper-logs:
  spark-apps:
  spark-data:
  cassandra-data:
  hdfs-namenode:
  hdfs-datanode:
  flink-apps:
  flink-data:

networks:
  network:
    name: network
    driver: bridge
